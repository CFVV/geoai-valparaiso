# macOS
.DS_Store

# Bitácora local de Claude Code (contexto de desarrollo/traspaso, NO es
# documentación pública del proyecto — vive solo en disco, nunca en git)
CLAUDE.md

# Python
__pycache__/
*.pyc
.pytest_cache/

# Datos pesados / derivados (reproducibles vía GEE, no viven en git — ver DATOS.md)
*.pkl
*.tif
*.tif.aux.xml
cambio_urbano/s2_mosaics/
cambio_urbano/lulc_io/
cambio_urbano/outputs/
incendios/MODIS/
incendios/outputs/

# NOTA: incendios/hexGrid_200m_incendiosValpo.gpkg (capa Miranda, 4.6 MB) NO se
# excluye — es dato histórico NO reproducible (no viene de GEE ni de ningún
# script) y debe quedar versionado en el repo. Ver DATOS.md.

# NOTA: cambio_urbano/data/tiles_priority.gpkg y
# cambio_urbano/data/subtiles_250m_change_classification.gpkg NO se excluyen
# — los generó un notebook exploratorio (no el pipeline) y no son
# reproducibles corriendo run_pipeline.py. Sin ellos el pipeline no arranca
# desde un clon limpio. Deben quedar versionados. Ver DATOS.md.

# Credenciales (ninguna vive en el repo hoy; regla defensiva)
*credential*
service_account*.json
.env

# RoofNet: checkpoint fine-tuned (1.7 GB, licencia xBD CC-BY-NC-SA no comercial,
# distribuido solo vía Kaggle con cuenta double-blind-review — no redistribuible
# desde este repo público) y su cache de RemoteCLIP (auto-descargado de HF).
# roofnet_finetuned.pth es un symlink local a ~/Documents/CIGIDEN/Roofnet/ —
# ver roofnet-eval/FINDINGS.md sección 1.
experiments/phase2-drone-vulnerability/roofnet-eval/scripts/roofnet_finetuned.pth
experiments/phase2-drone-vulnerability/roofnet-eval/scripts/checkpoints/
experiments/phase2-drone-vulnerability/roofnet-eval/scripts/roofnet_metadata.csv

# Chips crudos (imágenes PNG) de los 215 chips de ground truth QP1+VM8: nunca
# se subieron (decisión de la integración RoofNet, ver CLAUDE.md) -- viven
# localmente como symlinks a ~/Documents/CIGIDEN/experimentos_roofnet/{chips,vm8/chips}/,
# los usan exp11/exp12 para features y clasificación roofnet_ft.
experiments/phase2-drone-vulnerability/roofnet-eval/scripts/chips
experiments/phase2-drone-vulnerability/roofnet-eval/scripts/vm8/

# Este repo es independiente de los árboles de trabajo originales — nunca deben
# anidarse ni rastrearse aquí.
geoai-valpo/
MLxWildfires_valparaiso/
